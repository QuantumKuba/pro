# Dev Preview

Run a local preview of the demo app (uses your `.env` VITE_POLYGON key):

```bash
npm run dev
```

This starts Vite with the `dev/` directory as the server root. Open http://localhost:5173/ to view.
